/* supabase-config.js — Supabase 连接配置
 *
 * 部署到生产前请填入你的 Supabase 项目凭证：
 *   1. 到 https://supabase.com 创建项目（免费层够用）
 *   2. 项目 Settings → API → URL 和 anon public key
 *   3. 把下面两个空字符串填进去
 *
 * 未填写时（开发期 / demo）应用以 mock 模式运行：
 *   - 任何邮箱密码可登录
 *   - 数据仅存浏览器 localStorage
 *   - 不会有任何外部网络请求
 */
window.SUPABASE_CONFIG = {
  url:     '',
  anonKey: '',
};
